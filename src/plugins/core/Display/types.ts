import { Template } from '@templates';

export type Nexus_Display = {
  id: number ;
  main: string;
  department: string;
  display: string;
  background: string;
  title: string;

  template: Template | null;
}

